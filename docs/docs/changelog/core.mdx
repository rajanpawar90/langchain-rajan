# langchain-core

## 0.1.7 (Jan 5, 2024)

### Deprecated

- `BaseChatModel`: `__call__`, `call_as_llm`, `predict`, `predict_messages`. Use `BaseChatModel.invoke` instead.
- `BaseChatModel`: `apredict`, `apredict_messages`. Use `BaseChatModel.ainvoke` instead.
- `BaseLLM`: `__call__`, `predict`, `predict_messages`. Use `BaseLLM.invoke` instead.
- `BaseLLM`: `apredict`, `apredict_messages`. Use `BaseLLM.ainvoke` instead.

### Fixed

- Restrict recursive URL scraping: [#15559](https://github.com/langchain-ai/langchain/pull/15559)

### Beta

- `langchain_core.load.load`, `langchain_core.load.loads`
- `langchain_core.beta.runnables.context.ContextGet`, `langchain_core.beta.runnables.context.ContextSet`
