pip install langchain-community


from langchain_community.vectorstores import LanternVectorstore


vectorstore = LanternVectorstore(conn)
vectors = [(0, "your text".encode()), (1, "another text".encode())]
vectorstore.add_vectors(vectors)


query = "search text".encode()
results = vectorstore.semantic_search(query, k=5)
