from langchain_community.document_loaders.larksuite import LarkSuiteDocLoader

# Initialize the loader
loader = LarkSuiteDocLoader(access_token="your_access_token")

# Load a document
document = loader.load_document("document_id")
